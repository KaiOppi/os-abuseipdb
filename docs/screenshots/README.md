# Screenshot slots

Drop these four PNG files here (just capture the WebGUI in your browser, 1280×720 or similar):

| Filename | What to capture |
|---|---|
| `01-general.png` | Firewall → AbuseIPDB → **General** tab with `Plugin enabled` ticked and a (redacted) API key. Ideally showing the info banner at the top with a populated blocklist count. |
| `02-selfcare.png` | Firewall → AbuseIPDB → **Self-Defense** tab with a few entries in the "Currently blocked" table (IP, Added, Expires). |
| `03-log.png` | Firewall → AbuseIPDB → **Log** tab with a handful of recent report rows. |
| `04-widget.png` | Lobby → Dashboard → AbuseIPDB widget (after adding it), showing stats. |

Once committed, the images render automatically in the top-level README.md.
